import java.io.*;import java.util.*;public class gradebook {	public static int NumberOfStudents;	public static int NumberOfExams;	public static void main(String []args) throws IOException{		ArrayList<String> names = new ArrayList<>();		ArrayList<ArrayList<Integer>> listOfGrades = new ArrayList<ArrayList<Integer>>();		loadArrayLists(names, listOfGrades);				Header();		Options(names, listOfGrades);	}//end Main		public static void loadArrayLists(ArrayList<String> names, ArrayList<ArrayList<Integer>> listOfGrades) throws IOException	{		BufferedReader inFile;		inFile = new BufferedReader(new FileReader("inFile.txt"));		NumberOfStudents = Integer.parseInt(inFile.readLine());		NumberOfExams = Integer.parseInt(inFile.readLine());;		for(int i = 0; i < NumberOfStudents; i++)		{			listOfGrades.add(new ArrayList<Integer>()); //adds an arraylist to the listOfGrades arraylist to the "i" position			names.add(inFile.readLine());//add a name to the names arraylist			ArrayList<Integer> grades = listOfGrades.get(i); //retrieves the array list that was just created at beggining of for loop			for(int j = 0; j < NumberOfExams; j++)			{				grades.add(Integer.parseInt(inFile.readLine())); //adds a grade to the arraylist within the listOfGrades arraylist			}		}	}		public static void Header(){		System.out.println("Welcome to GradeBook 3.0");		System.out.println("With Elliot and Vazquez");	}//end Header		public static void  Options(ArrayList<String> names, ArrayList<ArrayList<Integer>> grades)throws IOException{		Scanner in = new Scanner(System.in);		int option;		boolean run = false;		do{		System.out.println("\nWhat would you like to do?" +				"\n1) Compute the average for all students?" +				"\n2) Compute the average for all exams?" +				"\n3) Add a new student?" +				"\n4) Add a new exam?" +				"\n5) Print the whole gradebook?" +				"\n6) Compute the average of one student?" +				"\n7) Compute the average for one exam?" +				"\n9) Exit the program?");		option = in.nextInt();		switch (option){		case 1:		{			AllStudents(names, grades);			run = true;			break;		}//end case 1		case 2:		{			AllExams(names, grades);			run = true;			break;		}//end case 2		case 3:		{			AddNewStudent(names, grades);			run = true;			break;		}//end case 3		case 4:		{			AddNewExam(names, grades);			run = true;			break;		}//end case 4		case 5:		{			GradeBook(names, grades);			run = true;			break;		}//end case 5		case 6:		{			StudentAverage(names, grades);			run = true;			break;		}//end case 6		case 7:		{			ExamAverage(names, grades);			run = true;			break;		}//end case 7		case 9:		{			SaveData(names, grades);			System.out.println("Saving progress....");			System.out.println("Saving progress....");			System.out.println("Saving progress....Saved!");			System.out.println("Thank you and see you soon!");			run = false;			break;		}//end case 9		}//end switch		}while(run);	}//end Options		public static void GradeBook(ArrayList<String> names, ArrayList<ArrayList<Integer>> listOfGrades){		System.out.print("Names ");		for(int count = 0; count <=NumberOfExams; count++){			System.out.print("Exam " + (count +1) + " ");		}		System.out.println();		for(int count=0; count <= NumberOfStudents; count++){			System.out.print(names.get(count) + " ");			if(names.get(count).charAt(0) == 'B' || names.get(count).charAt(0) == 'D'){				System.out.print(" ");			}						for(int counter=0; counter <= NumberOfExams; counter++){				System.out.print(listOfGrades.get(count).get(counter) + "     ");			}//for loop counter			System.out.println();		}//for loop count	}//end GradeBook Method		public static void StudentAverage(ArrayList<String> names, ArrayList<ArrayList<Integer>> listOfGrades)throws IOException {		BufferedReader in = new BufferedReader(new InputStreamReader (System.in));		System.out.println("Which student's average would you like? Please enter a number.");		for(int i = 0; i < NumberOfStudents;i++)		{			System.out.println((i+1) + ") " + names.get(i));		}		int response = Integer.parseInt(in.readLine());		int total = 0;		for (int j = 0; j < NumberOfExams; j++)		{			total = total + listOfGrades.get(response -1).get(j);		}		double average = total / NumberOfExams;		System.out.println("The average for " + names.get(response-1) + " is: " + average);		}//end StudentAverage		public static void ExamAverage(ArrayList<String> names, ArrayList<ArrayList<Integer>> listOfGrades){		Scanner in = new Scanner(System.in);		//int Exam;		double total = 0;		double average;		System.out.println("Which exam would you like to take the average of? Please enter a number.");		int response = in.nextInt();		for(int outer = 0; outer < NumberOfStudents; outer++)		{			total = total + (double)listOfGrades.get(outer).get(response -1);		}		average = total / (double)NumberOfStudents;		System.out.println("The average is: " + average);	}//end ExamAverage		public static void AllStudents(ArrayList<String> names, ArrayList<ArrayList<Integer>> listOfGrades){		double average = 0;			for(int count= 0; count <= NumberOfStudents; count++){			System.out.print("The average of " + names.get(count) + " is ");			for(int counter= 0; counter <= NumberOfExams; counter++){				average = average + ((double)listOfGrades.get(count).get(counter));			}			average = average / (NumberOfExams+1);			System.out.println(average + ".");			average = 0;		}				}//end AllStudents		public static void AllExams(ArrayList<String> names, ArrayList<ArrayList<Integer>> listOfGrades){		double average = 0;				for(int count= 0; count <= NumberOfExams; count++){			System.out.print("The average of Exam " + (count+1) + " is ");			for(int counter= 0; counter <= NumberOfStudents; counter++){				average = average + ((double)listOfGrades.get(counter).get(count));			}			average = average / (NumberOfStudents+1);			System.out.println(average + ".");			average = 0;		}	}//end AllExams		public static void AddNewStudent(ArrayList<String> names, ArrayList<ArrayList<Integer>> listOfGrades)throws IOException{	System.out.println();	System.out.println("Enter New Student:");		Scanner input = new Scanner(System.in);				names.add(input.next());		listOfGrades.add(new ArrayList<Integer>());		System.out.println("What were his/her grades?");		for(int counter=0; counter <= NumberOfExams; counter++){			System.out.print("Test " + (counter+1) + ": ");			listOfGrades.get(NumberOfStudents).add(input.nextInt());					 System.out.println();		 		}//for loop counter				NumberOfStudents = NumberOfStudents + 1;	}//end Add New Student		public static void AddNewExam(ArrayList<String> names, ArrayList<ArrayList<Integer>> listOfGrades)throws IOException{		Scanner input = new Scanner(System.in);					System.out.println("Please Enter All Students Grades for this Exam.");		for(int count=0; count <= NumberOfStudents; count++){			System.out.print(names.get(count) + ": ");		for(int counter= NumberOfExams; counter <= NumberOfExams; counter++){			listOfGrades.get(count).add(input.nextInt());		}//for loop counter		}//for loop count		NumberOfExams = NumberOfExams + 1;	}//end Add New Exam		public static void SaveData(ArrayList<String> names, ArrayList<ArrayList<Integer>> listOfGrades)throws IOException{		PrintWriter outFile;		outFile = new PrintWriter(new FileWriter("outFile.txt"));		outFile.println(NumberOfStudents);		outFile.println(NumberOfExams);		for(int count = 0; count <= NumberOfStudents; count++){			outFile.println(names.get(count));			for(int counter = 0; counter <= NumberOfExams; counter++){				outFile.println(listOfGrades.get(count).get(counter));			}//for loop counter		}//for loop count		outFile.close();	}//end SaveData}//end GradeBook